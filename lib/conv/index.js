/**
 * Conversion Types:
 * Numbers (ASCII, HEX, DEC, BIN, OCTAL, ROM_NUM)
 * 
 */

//Imports


module.id = "conversion";

module.exports = {
    test_module = function () {
        console.log("Module Test Was Successful");
    };
};